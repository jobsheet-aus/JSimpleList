JSimpleList_CONTEXT.txt

PROJECT
-------
Name: JSimpleList
Package: au.com.jobsheet.jsimplelist
Local project: D:\SimpleList
Public repository: https://github.com/jobsheet-aus/JSimpleList
Purpose: Small local-first native Android list app. Version 2.0 generalises the original fixed To-do and Shopping lists into user-named dynamic lists and adds optional online sync/sharing.
Intrinsic list kinds: TODO, SHOPPING and DISCUSSION. Shopping differs by having a Qty field.
Google Play is the intended public distribution channel.

CORE PRODUCT PRINCIPLES
-----------------------
- Keep JSimpleList deliberately small, direct and local-first.
- Local-only use requires no account and must continue to work without internet or backend availability.
- Signing in must never automatically upload existing local lists.
- A local list becomes online only through an explicit user action for that specific list.
- Online/shared lists remain fully cached in Room and usable offline.
- Preserve local UUIDs as server UUIDs for online-backed lists/items.
- List ownership is absolute; the creator remains owner.
- One authenticated account at a time per Android device is the deliberate UX model.
- Back cancels/closes the most local active operation first.

STACK
-----
Android native app: Kotlin + Jetpack Compose
Minimum SDK: 26
Target SDK: 36
Compile SDK: 36.1
Android Studio bundled JDK 21
AndroidX Core KTX pinned to 1.18.0 because 1.19.0 requires compile SDK 37
Room 3 / androidx.room3 with AndroidSQLiteDriver
SharedPreferences retained for font scale, one-time legacy 1.1 JSON import, last active list ID and persistent per-install client UUID
Supabase/PostgreSQL for optional online services
supabase-kt 3.2.1 Auth/PostgREST/Realtime + Ktor CIO 3.2.1
Manrope bundled with the APK
Compose Foundation explicit dependency for HorizontalPager
Android 2.0 manifest has INTERNET permission; released 1.1 did not

RELEASED 1.1 / HISTORICAL ARCHIVE
---------------------------------
Released 1.1 is the fixed-list local-only app:
- To-do and Shopping tabs
- Shopping Qty
- add/tick/untick/delete/edit
- completed items remain visible below incomplete items
- local SharedPreferences/JSON persistence
- pinch-to-resize list/content text
- horizontal swipe between fixed To-do/Shopping pages
- no account, advertising, analytics or server connection

Release metadata:
    versionCode 2
    versionName 1.1
    build date 21 August 2026

Google Play App Signing is enabled.
JSimpleList 1.1 remains the intended `Classic local-only release` for historical/local-only use.
Local archive path:
    D:\SimpleList\releases\JSimpleList-1.1-play-signed.apk
The `/releases/` directory is intentionally git-ignored.
Google Play production signing-certificate SHA-256:
    ce9a34612fb932d2ed0200a4e0d072648c91f21ef1224fba9c5d099fda973380

2.0 LOCAL DATA MODEL
--------------------
Room schema version 6 contains `lists`, `items` and `list_accounts`.

ListEntity:
- UUID id
- user-controlled name
- kind TODO / SHOPPING / DISCUSSION
- persisted position
- createdAt / updatedAt
- onlineState convenience value LOCAL / ONLINE_OWNER / ONLINE_MEMBER; account-specific authority lives in `list_accounts`

ItemEntity:
- UUID id and listId
- description
- quantity nullable; Shopping only
- completed
- position
- createdAt / updatedAt
- deletedAt nullable tombstone
- createdByUserId / updatedByUserId nullable attribution

ListAccountEntity / `list_accounts`:
- composite primary key `(listId, accountId)`
- accountId is authenticated Supabase user UUID
- onlineState ONLINE_OWNER or ONLINE_MEMBER for that account/list relationship
- the same cached list may have relationships for multiple accounts on one device
- a genuine device-local list has no `list_accounts` row

Visibility contract:
- signed out: genuine local lists only
- signed in as account A: local lists plus online lists related to account A
- another account's cached online lists must not leak into the active account UI

Legacy upgrade:
- 1.1 SharedPreferences JSON imports only when Room has no lists and genuine legacy keys exist.
- Genuine Google Play-signed 1.1 -> 2.0 in-place upgrade testing passed on device and preserved representative items, quantities, completion, ordering and font scale.

2.0 LIST MODEL / UI
-------------------
- Every list has a user-controlled name independent of kind; names need not be unique because UUID is identity.
- New-list flow: Type -> Name -> Create.
- Defaults: `To-do list`, `Shopping list`, `Discussion points`.
- Zero lists is valid.
- One current list fills the screen; horizontal swipe moves between lists.
- Manage lists is the canonical arbitrary-list navigation/admin surface.
- Online/shared lists show the muted chain/link icon; local lists are unmarked.
- Pull-to-refresh performs online discovery/reconciliation.
- Last active list is persisted/restored safely when still visible.
- Bulk Uncheck all is implemented in the list column-heading row and has conservative Undo.
- New items insert at the top; incomplete items display before completed items.
- Existing items edit inline; item-description cursor starts at the end; Shopping Qty selects its existing value on focus.
- Large-text editing was improved before release; edit text is capped at 22sp and fields can grow vertically.

AUTH / PROFILE / ACCOUNT
------------------------
- `AuthRepository.kt` uses Supabase six-digit email OTP authentication.
- OTP delivery through Supabase Auth -> Resend SMTP -> `jsimplelist@jobsheet.com.au` is proven.
- Session persists across normal app close/reopen.
- Re-authenticating the same email returns the same Supabase user UUID.
- `ProfileRepository.kt` stores profiles keyed by auth.users.id.
- Display names are non-unique, trimmed and limited to 1-50 characters.
- Sign out is non-destructive and separate from account deletion.
- 2.0 now has a clear hamburger-menu account path: signed out shows `Sign in`; signed in shows `Online account`; the existing account dialog is titled `Online account`.
- This sign-in-path change does not alter the local-first rule: signing in does not make local lists online.
- Account deletion remains a separate destructive action.
- Raw network/auth/server exception text must never be rendered to users. VersionCode 7 replaced all remaining direct `.message` UI paths with fixed safe messages after a Supabase/Ktor exception exposed request/JWT/header detail in an internal Play test build.

ONLINE ACCOUNT / OWNERSHIP
--------------------------
- Online identities are persistent Supabase Auth users.
- Shared/online relationships belong to authenticated users; local lists belong to the device.
- Account deletion is implemented through privileged hosted backend logic; no service-role/admin secret ships in Android/browser.
- Android account-deletion cleanup is account-scoped.
- Owned online/shared lists are destroyed when the owner's account is deleted.
- Non-owner account deletion removes membership but preserves contributed items in another owner's list because those items belong to the list.

SUPABASE / SHARING BACKEND
--------------------------
Canonical migrations live under `supabase/migrations` and are applied through `npx supabase ...`.
Hosted project ref:
    xislrkkifqayznsgwzno

Important deployed components:
- custom `jsimplelist` schema exposed for Data API
- profiles, lists, memberships, invitations, item tombstones, list discovery/sync, account cleanup and attribution
- persistent notifications + durable `push_outbox`
- Firebase device registration using FID and persistent per-install client UUID
- recipient maturity classification ACTIVE_ANDROID / KNOWN_USER / GREEN
- purpose-specific GREEN invitation handoff
- hosted Edge Functions: `delete-account`, `invite-list`, `send-push-outbox`, `invitation-handoff`

GREEN invitation flow:
- `invite-list` emails GREEN recipients one opaque `https://jslist.jobsheet.com.au/invite?h=...` continuation.
- Handoff token is 32 random bytes; only SHA-256 is stored; expiry is 24 hours.
- Passive browser inspect grants nothing; explicit `Join list` is the redemption boundary.
- Server bootstraps/redeems the invited Supabase identity and then calls authoritative `accept_list_invitation()`.
- Private-browser testing with a never-used email proved the GREEN flow end to end.

Member-join notification fan-out:
- Implemented in migration `20260902120431_notify_all_members_on_join.sql`.
- Successful `accept_list_invitation()` creates an `invitation_accepted` notification for every other active list member, excluding the joining member.
- Notification insertion is non-authoritative/non-blocking and feeds the durable push outbox.
- Migration is applied to hosted Supabase.

Push state:
- FCM invitation, accepted/join and declined pushes work on real Android devices.
- Notification taps route account-safely to relevant invitation/list.
- The removed automatic in-app `Sharing update` notification fallback must not be reintroduced.

LIST SYNC / REALTIME
--------------------
- Local lists remain local and are never uploaded merely because the user signs in.
- `discoverOnlineLists()` rebuilds current-account relationships from authoritative online-list discovery.
- Remote lists import/reconcile into Room cache and authoritative snapshots refresh items.
- Add/edit/tick/untick write Room first then push for online lists.
- Online item deletion uses local/server tombstones.
- Owner Delete remains server-first; member Leave remains server-first; local Delete remains Room-only.
- Per-list private realtime topic: `jsimplelist:list:<list UUID>`.
- Realtime payloads signal `list_changed`; clients reload authoritative state rather than trusting payload rows.
- Origin client UUID suppresses own-origin refresh loops.
- Failed ordinary online writes retain the Room change; durable retry/reconciliation remains future work.

ANDROID INVITATION FOLLOW-UP
----------------------------
- Browser GREEN onboarding is proven.
- Android `/auth/invite` handling does not yet implement the final opaque `h` one-click capability.
- App-not-installed -> Play install -> resume handling is not yet proven.
- Verification-code rate-limit fields/RPCs from the abandoned two-step GREEN design still require later cleanup or deliberate repurposing.

BROWSER CLIENT
--------------
Browser app:
    https://jslist.jobsheet.com.au
Source:
    web/
Hosting:
    DigitalOcean App Platform Static Site, SYD1

Current proven browser behaviour:
- Email OTP sign-in and persistent session.
- Discovers active online lists and opens authoritative snapshots.
- Item add/edit/tick/untick/delete syncs with Android.
- Bulk Uncheck all implemented.
- Inline list rename implemented through controlled RPC.
- Private per-list realtime works with origin suppression.
- Browser exposes only online-backed lists; local-only Android lists never appear.

Browser follow-up still planned:
- browser list creation
- complete list management including delete
- authenticated browser self-service account deletion
- mature sharing/member-management UX
- immediate eviction/reload when selected remote list is deleted
- account-level realtime discovery

2.1 ANDROID / MAINTENANCE FOLLOW-UP
----------------------------------
Do not retroactively change the submitted 2.0 build unless Google review identifies a genuine release blocker.

Planned work:
- Detect equivalent existing items when adding and prevent/handle accidental duplicate entries; decide final UX deliberately because intentional duplicates can be legitimate.
- Allow list renaming by tapping/clicking the visible list title.
- Review navigation so Android Back from an open list goes to Manage lists before exiting; also evaluate whether Manage lists should become the startup/home surface instead of always restoring the last active list.
- Fix the observed navigation bug after online-list discovery where the first tap on a newly pulled shared list can open a different list. Current hypothesis is stale pager/index state after the visible list collection changes; inspect exact selector/pager code before changing it.
- Streamline sharing into a task-driven flow: `Manage lists -> Share this list -> [sign in if required] -> make only that list online -> invite person -> done`, automatically resuming after authentication rather than requiring the user to restart the share action.
- Add coloured list-type icons for To-do, Shopping and Discussion with an extendable design.
- Add finger drag-and-drop item reordering while preserving sensible incomplete/completed grouping.
- Add `Delete all items` with confirmation, distinct from `Uncheck all`.
- Detect `http://` and `https://` links in item descriptions and make the URL portion tappable without previews initially.
- Remove unnecessary left padding from inline edit rows.
- Possible preference: allow unused list types to be hidden from the New list chooser without affecting existing lists.
- Manual list ordering remains planned.

Release optimisation / Google Play maintenance:
- Google Play flags the current production/release build for low optimisation/obfuscation and recommends R8/ProGuard, deobfuscation mapping and native debug symbols.
- Do not enable shrinking casually on the already-tested 2.0 build.
- In 2.1/maintenance, inspect current release configuration, enable `isMinifyEnabled = true` and `isShrinkResources = true` with only necessary keep rules, build a release AAB, and real-device test auth, Room, Supabase sync/sharing, notifications, account deletion and linked flows.
- Upload the R8 mapping/deobfuscation file and native debug symbols where the build produces them/Play expects them.
- Re-check Play Console optimisation status after the tested optimised release.

PUBLIC WEBSITE / LEGAL / PLAY STORE
-----------------------------------
Public product page:
    https://jobsheet.com.au/jslist/
Legacy landing redirect:
    https://jobsheet.com.au/jsimplelist/
Browser app:
    https://jslist.jobsheet.com.au
Privacy policy:
    https://jobsheet.com.au/jsimplelist-privacy.html
Terms:
    https://jobsheet.com.au/jsimplelist-terms.html
Account deletion resource:
    https://jobsheet.com.au/jsimplelist-delete-account.html

Google Play Data Safety for 2.0:
- Name, Email address, User IDs, Other user-generated content, Device or other IDs
- collected, not shared with third parties
- encrypted in transit
- collection optional because local-only use requires none of it
- account deletion links to the public deletion resource

Google Play App Links:
- production `assetlinks.json` includes the actual Play signing certificate SHA-256 plus local release certificate
- Android `pm get-app-links au.com.jobsheet.jsimplelist` reports `jslist.jobsheet.com.au: verified`

Store listing text:
- App name remains `JSimpleList`.
- Short description: `Simple list manager with optional private sharing and browser access`.
- Full description covers dynamic To-do/Shopping/Discussion lists, local-first use, optional online sync/sharing/browser access and recurring-list reuse with Uncheck all.

2.0 Play assets/submission:
- Main store listing updated with a 2.0 app icon, feature graphic and phone screenshots.
- The final screenshot/feature-art set used image-generation/AI editing and was conservatively declared as AI-generated/edited in Play Console.
- Production release uses versionCode 7 / versionName 2.0.
- New install size shown by Play is about 11.5 MB; larger than 1.1 because 2.0 includes Room/Supabase/network/realtime/auth functionality.
- Play warnings at submission: increased download size, no deobfuscation mapping because R8/ProGuard is not enabled, and no native debug symbols for bundled native code. These were warnings, not blockers, and are recorded for 2.1/maintenance.
- Managed publishing is OFF.

CURRENT RELEASE CHECKPOINT - 5 SEP 2026
---------------------------------------
Current Android metadata/source:
    versionName 2.0
    versionCode 7
    BUILD_DATE 4 September 2026

Current pushed source checkpoint before documentation closeout:
    563d380 Prevent raw network errors reaching the UI

Other immediate checkpoints:
    b119082 Add clear online account sign-in path
    8e5e802 Clarify VS Code search and replace indentation rule

Validated release state:
- versionCode 7 release AAB builds successfully
- versionCode 7 was installed through Google Play Internal testing and tested successfully on-device
- signed-out hamburger shows `Sign in`; signed-in hamburger shows `Online account`
- OTP sign-in, account screen, online-list discovery/sharing and notification prompt were tested successfully
- raw Supabase/Ktor exception text is no longer rendered in the UI
- genuine Play-signed 1.1 -> 2.0 upgrade had already passed
- Play production App Links are verified
- store listing, artwork/screenshots and Data Safety were updated
- Production release `7 (2.0)` and all listing changes were submitted together
- Google Play Console status at session close: `Changes in review`
- Managed publishing is OFF, so approved changes should publish automatically unless Google requires another action

Release submission details:
- Production release includes versionCode 7 / versionName 2.0 and does not include the old 1.1 bundle.
- Publishing overview showed seven changes together: Production rollout plus short description, full description, app icon, phone screenshots, feature graphic and Data Safety.
- Developer verification/app-signing-registration status should still be checked/completed before the 30 September 2026 deadline if not already completed.

RELEASE SCOPE DECISION
----------------------
- 2.0 is submitted and should remain frozen while under review unless Google identifies a genuine blocker.
- Do not widen 2.0 into the 2.1 roadmap now.
- User intends to return to JobSheet for a while after this milestone.
- On return to JSimpleList, first check Play review/publication status and verify the live 2.0 update if approved; then continue 2.1/maintenance work.

FUNDING / COST FUTURE-PROOFING
------------------------------
- Local-only use should remain free.
- Online sync/sharing/browser access may initially be free while costs are negligible.
- If recurring backend cost becomes meaningful, preferred model is a small annual fee, notionally around US$5/year, rather than a monthly subscription.
- Voluntary donations/support may exist separately.
- Do not hard-wire billing into list operations; keep an entitlement boundary for online service.

PROJECT WORKING PROTOCOL / DOCUMENTS
------------------------------------
- `docs/chatgpt/JSimpleList_RULES.txt` is the authoritative durable working/source-editing/validation/Git/session protocol.
- `docs/chatgpt/JSimpleList_CONTEXT.txt` is the concise current architecture/state/continuation reference.
- ChatGPT should perform project-document maintenance directly when repository/file tooling permits.
- Remove/rewrite stale current-state and NEXT TASK material instead of accumulating contradictory history.
- Session closeout follows `JSimpleList_RULES.txt`.

CMD / TOOLING
-------------
Assume Windows CMD, normally from:
    D:\SimpleList>

Use CMD-compatible one-line commands and `&&` when dependent commands should run sequentially.
Always use `git --no-pager` where applicable.
Normal debug workflow:
    build_debug && install_debug
Underlying debug build if needed:
    gradlew.bat assembleDebug
Release AAB:
    gradlew.bat bundleRelease
Real phone remains preferred over emulator.
Use `audit` before source edits and exact source blocks/indentation for manual replacements.
Explicitly stage intended files; avoid broad `git add .`.

SIGNING / FILE SAFETY
---------------------
Permanent local signing key:
    D:\SimpleList\keys\jsimplelist-release.jks
Alias:
    jsimplelist
Local release certificate SHA-256:
    fc11217128f365366d4c3f988a2536d40251f6e71b5275b611896c749206af59

Never commit .jks files, signing.properties, signing passwords, Supabase secret/service-role keys, Resend API keys, Firebase service-account secrets or generated release output.

PUBLIC REPOSITORY / LICENCE
---------------------------
Repository:
    https://github.com/jobsheet-aus/JSimpleList
Branch:
    main
Repository is public.
Current licence: MIT.
Copyright owner:
    21 TWELVE CONSULTING PTY LTD
Existing MIT releases retain their granted MIT rights if future versions use different licensing terms.

NEXT TASK
---------
JSimpleList is deliberately paused while the user returns to JobSheet.

When returning to JSimpleList:
1. Read `docs/chatgpt/JSimpleList_RULES.txt` and this file first.
2. Check Google Play Console review/publication status for Production versionCode 7 / versionName 2.0.
3. If approved/live, install/update from Production and perform a short smoke test of the public build.
4. Check/complete Google Play developer verification/app-signing registration before 30 September 2026 if still outstanding.
5. Begin the 2.1/maintenance pass from the recorded navigation/sharing/duplicate-item/R8 work above rather than rediscovering it from chat history.

Do not reintroduce the removed JSimpleList in-app `Sharing update` notification fallback.

SOURCE-EDITING REMINDER FOR NEXT SESSION
----------------------------------------
Read and follow `docs/chatgpt/JSimpleList_RULES.txt`; it is authoritative.
