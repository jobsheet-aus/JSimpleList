JSimpleList_CONTEXT.txt

PROJECT
-------
Name: JSimpleList
Package: au.com.jobsheet.jsimplelist
Local project: D:\simplelist
Public repository: https://github.com/jobsheet-aus/JSimpleList
Purpose: Small local-first native Android list app. Version 2.0 generalises the original fixed To-do and Shopping lists into user-named dynamic lists and adds optional online sync/sharing.
Intrinsic list kinds: TODO, SHOPPING and DISCUSSION. Shopping differs by having a Qty field.
Google Play is the intended public distribution channel.

CORE PRODUCT PRINCIPLES
-----------------------
- Keep JSimpleList deliberately small, direct and local-first.
- Local-only use requires no account and must continue to work without internet or backend availability.
- Signing in must never automatically upload existing local lists.
- A local list becomes online only through an explicit user action for that specific list.
- Online/shared lists remain fully cached in Room and usable offline.
- Use one common list model for household and business/work sharing; do not create a separate team hierarchy unless it becomes genuinely useful.
- Preserve local UUIDs as server UUIDs for online-backed lists/items; never create a second identity for the same record.
- List ownership is absolute. The creator remains owner; ownership is not transferred or inherited.
- Back cancels/closes the most local active operation first. Only when nothing transient is active should Back be allowed to leave JSimpleList.
- Existing-value editing behaviour is field-specific: short replacement-style values such as Qty/default names select all, while item descriptions place the cursor at the end so small wording edits do not risk replacing the whole description.

STACK
-----
Android native app
Kotlin + Jetpack Compose
Gradle wrapper
Minimum SDK: 26
Target SDK: 36
Compile SDK: 36.1
Android Studio bundled JDK 21
AndroidX Core KTX pinned to 1.18.0 because 1.19.0 requires compile SDK 37
Room 3 / androidx.room3 with AndroidSQLiteDriver for 2.0 local persistence
SharedPreferences retained for font scale, one-time legacy 1.1 JSON import, last active list ID and a persistent per-install client instance UUID
Supabase/PostgreSQL selected for optional 2.0 online services
supabase-kt 3.2.1 Auth/PostgREST/Realtime + Ktor CIO 3.2.1. CIO is required because the previous Ktor Android engine did not provide the WebSocket capability needed by Supabase Realtime
Manrope bundled with the APK
Compose Foundation explicit dependency for HorizontalPager
Android 2.0 manifest has INTERNET permission; released 1.1 did not

RELEASED 1.1
------------
Released 1.1 is the fixed-list local-only app:
- To-do and Shopping tabs
- Shopping Qty
- add/tick/untick/delete/edit
- completed items remain visible below incomplete items
- local SharedPreferences/JSON persistence
- pinch-to-resize list/content text
- horizontal swipe between fixed To-do/Shopping pages
- incomplete counts in headings
- compact 48 dp entry controls
- bundled Manrope and JSimpleList logo/icon
- About dialog with version/build date, JobSheet/GitHub links, copyright and MIT wording
- no account, advertising, analytics or server connection

Release metadata:
    versionCode 2
    versionName 1.1
    build date 21 August 2026

JSimpleList 1.1 was submitted to Google Play Production. The context at submission time recorded it as in review. Google Play App Signing is enabled.

2.0 LOCAL DATA MODEL
--------------------
Room schema version 6 contains `lists`, `items` and `list_accounts`.

ListEntity:
- id: UUID string
- name: user-controlled
- kind: TODO / SHOPPING / DISCUSSION
- position: explicit persisted list order/cache position
- createdAt: epoch millis
- updatedAt: epoch millis used for list-metadata reconciliation
- onlineState: legacy/current-render convenience value `LOCAL`, `ONLINE_OWNER` or `ONLINE_MEMBER`; account-specific authority now lives in `list_accounts`

ItemEntity:
- id: UUID string
- listId: owning list UUID
- description
- quantity: nullable; Shopping only
- completed
- position: explicit persisted item order
- createdAt
- updatedAt
- deletedAt: nullable epoch millis tombstone for online-safe deletion
- createdByUserId: nullable authenticated creator UUID for shared-item attribution
- updatedByUserId: nullable authenticated UUID of the most recent editor

ListAccountEntity / `list_accounts`:
- composite primary key `(listId, accountId)`
- listId references `lists(id)` with ON DELETE CASCADE
- accountId is the authenticated Supabase user UUID
- onlineState is the role/state for that account on that list, currently `ONLINE_OWNER` or `ONLINE_MEMBER`
- the same cached list may therefore be owner for one account and member for another on the same device
- a genuinely device-local list has no `list_accounts` relationship rows

Visibility contract:
- local lists belong to the device and remain visible whether signed in or signed out
- shared/online lists belong to authenticated user relationships, not to the device as a whole
- signed out: show only genuine local lists
- signed in as account A: show local lists plus online lists related to account A
- account A's online lists must not remain visible merely because account B later signs in on the same device
- `loadVisibleLists(accountId)` projects the active account's relationship `onlineState` into the returned `ListEntity`
- legacy online rows with no `list_accounts` relationship are deliberately hidden until authoritative Supabase discovery associates them with the signed-in account

Room remains the local/cache copy used by the Android UI. Online-backed lists reconcile against authoritative Supabase discovery/snapshot results.

Legacy upgrade:
- Legacy 1.1 SharedPreferences JSON is imported only when the Room database has no lists and genuine `todo_items` / `shopping_items` keys exist.
- A fresh 2.0 install starts with zero lists and must not fabricate legacy To-do/Shopping lists.
- Genuine upgrade import creates ordinary To-do and Shopping ListEntity rows and preserves descriptions, quantities, completion and order.
- Room v4 added `lists.updatedAt`; migration initialises it from `createdAt`.
- Room v5 adds `list_accounts`; migration 4->5 creates the relationship table and indexes without guessing account ownership for pre-existing online cache rows.
- Room v6 adds nullable `createdByUserId` and `updatedByUserId` item attribution fields; migration 5->6 preserves all existing item data and leaves historic attribution unknown/null.
- Existing local data migrates safely through Room schema upgrades; item tombstones default to null.
- Legacy JSON is left untouched during import.
- `LegacySimpleListItem` exists only for import compatibility; new writes must not use the old JSON format.
- Before public 2.0 release, perform a real Google-signed update from installed 1.1 and verify preservation of To-do/Shopping items, quantities, completed states and font scale.

2.0 LIST MODEL / UI
-------------------
- Every list has a user-controlled name independent of its kind.
- Names do not need to be unique; UUID is identity.
- Valid examples: `Bunnings` / SHOPPING, `House jobs` / TODO, `Monday toolbox meeting` / DISCUSSION.
- New-list flow is Type -> Name -> Create.
- Default names: `To-do list`, `Shopping list`, `Discussion points`.
- Default names are fully selected ready to overwrite. Changing type updates/reselects only while the name is blank or still a previous default; custom text is preserved.
- Creating a list navigates directly into the newly created list using pager `requestScrollToPage`.
- Renaming changes only name; it must not alter UUID, kind, contents, sharing identity or membership.
- Zero lists is valid. Deleting the final list leaves a `No lists` state with a New list path.
- Permanent 1.1 tabs are removed in 2.0.
- One current list fills the screen; horizontal swipe moves to adjacent lists with natural ends.
- Manage lists remains the canonical arbitrary-list navigator/admin surface.
- Online/shared list identity is marked by the muted chain/link icon; local lists are unmarked.
- Pull-to-refresh performs discovery/reconciliation; visible `Refresh` text is removed.
- Back closes Manage lists before Android can leave the app.

Current account/menu/sharing UX:
- JSimpleList deliberately supports one authenticated account at a time per device for clarity.
- Hamburger contains `New list`, `Manage lists`, `Sharing`, dynamic `Sign in`/`Sign out`, signed-in `Delete account`, and `About`.
- When signed in, the Sign out menu item also shows the authenticated email address in smaller text.
- `Sign in` opens the existing sharing/auth dialog.
- `Sign out` uses a confirmation dialog: `Shared lists will be unavailable on this device until you sign in again. Local lists will remain available.`
- Sign-out reloads `loadVisibleLists(null)`, so shared lists disappear while genuine device-local lists remain.
- Sharing is non-destructive profile/account information. It shows `List sharing is enabled`, the signed-in email and a large tappable display name for editing.
- The older `Disable sharing` action has been removed. Account deletion is a separate explicit hamburger action.
- The prominent current-list action is now `Share list`; the former prominent `+ List` action has moved into the hamburger as `New list`.
- `Share list` distinguishes two intentions without exposing the term self-share:
  1. `Use this list on my other devices` / `Make available online` for the current authenticated account.
  2. `Invite someone` by email for another account.
- For an online list, the Share dialog explains that it is available wherever the signed-in account is used and provides the invitation email field.
- The owner-side invitation UI and backend send path are implemented. The recipient one-click onboarding experience is deliberately not considered finished.
- Detailed invitation/onboarding architecture is authoritative in `docs/chatgpt/JSimpleList_INVITATION_FLOW.txt`.

Item ordering:
- New items are inserted at the top by assigning a position below the current minimum.
- The active LazyColumn scrolls to the newly added item so it stays visible under the soft keyboard.
- Display ordering respects persisted `position`, then `createdAt`, while incomplete items are presented before completed items.
- Room v6 adds nullable `createdByUserId` and `updatedByUserId` item attribution fields; migration 5->6 preserves all existing item data and leaves historic attribution unknown/null.
- Toggling completion changes completion state, not item position.
- Explicit persisted position remains the basis for future manual item reordering.
- Manual item reordering should work for all list kinds.
- Discussion points may later render derived numbering from item order.
- Manual list ordering is also planned and should use persisted per-user membership/list position.
- Bulk uncheck is implemented as a checkbox in the list column-heading row, aligned directly above the item checkboxes. Tapping it unchecks all completed items using the normal item-update/sync path; temporary Undo restores only items not changed since the bulk operation.

Editing UX:
- Existing items edit inline.
- Existing item descriptions enter editing with the cursor at the end rather than selecting the whole description.
- Shopping Qty selects its entire existing value on focus so typing replaces it immediately.
- Shopping edit Qty is 48 x 48 dp; edit description is 48 dp high.
- Item Cancel logic is centralised and shared by visible Cancel and Android Back.

Visual/interaction decisions:
- Keep list rows compact without making controls fiddly.
- Use available phone width for item text; avoid unnecessary outer padding.
- Add/Delete remain visually secondary.
- Pinch resizes list/content text, not structural UI.
- Use Manrope throughout all Material typography roles and normal app text.
- Menus/dialogs use the same white surface family as the app.
- Use the JSimpleList logo blue/cyan colour family.
- User-facing informational phrases generally omit trailing full stops.
- Use `Email address`, not ambiguous `Email`.

AUTH / PROFILE FOUNDATION
-------------------------
Committed milestone:
    a25f1fe Add JSimpleList authentication and profile foundation

Implemented/current:
- Modular `AuthRepository.kt` using Supabase six-digit email OTP authentication.
- OTP delivery through Supabase Auth -> Resend SMTP -> `jsimplelist@jobsheet.com.au` is proven on real phones.
- Normal Magic link or OTP template uses the six-digit `{{ .Token }}` flow and includes JSimpleList landing-page and Google Play links.
- Auth session persists across normal app close/reopen; uninstalling the debug app removes the local session.
- Re-authenticating with the same email resolves to the same persistent Supabase user UUID.
- Startup now waits for Supabase Auth `SessionStatus.Initializing` to finish before reading the persisted session; this fixed the race where a valid restored session could appear signed out in the UI.
- Supabase-kt remains pinned at 3.2.1 because 3.7.0 was compiled with Kotlin 2.4 metadata and does not build against the current Kotlin 2.2.10 toolchain.
- A known supabase-kt 3.2.1 sign-out defect can throw on remote/network failure before clearing local persisted session. `AuthRepository.signOut()` tries normal `client.auth.signOut()` and falls back to the verified public `client.auth.clearSession()` method.
- One authenticated account at a time per device is the deliberate UX model.
- Modular `ProfileRepository.kt`; profiles are keyed by stable `auth.users.id`.
- Display names are user-chosen, non-unique, trimmed and limited to 1-50 characters.
- First-time authenticated users choose a display name; existing names load/edit normally.
- Sign out remains non-destructive and separate from account deletion.

ONLINE ACCOUNT / OWNERSHIP
--------------------------
- Online identities are persistent Supabase Auth users with stable UUIDs; local-only use remains account-free.
- Shared/online list relationships belong to authenticated users, not to the device globally. Local lists belong to the device.
- Multiple different accounts may use the same Android device sequentially; only the current account's shared lists should be visible, while device-local lists remain visible to either account.
- The same server list may legitimately have multiple `list_accounts` rows on one device, e.g. Account A owner and Account B member.
- `Delete account` is implemented as a separate destructive hamburger action and uses the privileged hosted deletion backend.
- Android account-deletion cleanup is account-scoped: it removes the deleted account's `list_accounts` relationships, removes cached lists that account owned, keeps genuine device-local lists, and does not convert unrelated online cache rows into local lists.
- Disposable-account end-to-end testing succeeded: the Auth user/profile were removed, owned online list data was removed as expected, Android returned to signed-out/local-only state, and no unexplained orphan online data was found.
- Owned online/shared lists are destroyed when the owner's online account is deleted; other members consequently lose access.
- Non-owner account deletion removes membership but deliberately preserves items that user contributed to another owner's shared list because items belong to the list, not to the contributor identity.
- A non-owner member may leave a shared list but may not destroy it; an owner may remove members or destroy the list.
- Account deletion remains a controlled privileged server-side operation; no service-role/admin secret may ship in Android/browser.
- Google Play release work still requires the external/web account-deletion resource/page in addition to the in-app path.
- Dormant empty-account cleanup is deliberately deferred; do not auto-purge accounts merely because they currently own no online lists.

SUPABASE BACKEND FOUNDATION
---------------------------
Committed milestones include:
    ee9e561 Add Supabase backend foundation
    14f7277 Add Supabase Android client
    76fdf45 Add safe online item tombstones
    7c98a9c Add realtime list refresh and sync signalling
    e829cad Add online list rename broadcasts
    32c350c Add online list discovery RPC
    6f41c0e Add online list delete and leave RPCs

Supabase:
- Custom schema `jsimplelist` exposed for the Data API.
- SQL migrations 001-027 live under `migrations/`; migrations 026 and 027 were applied to the hosted project on 31 Aug 2026.
- Migration 026 fixes accepted-member list position assignment.
- Migration 027 adds nullable `created_by_user_id` / `updated_by_user_id` item attribution, database-enforced attribution from `auth.uid()`, and exposes attribution in list sync snapshots.
- Migration 024 adds authenticated online-account data cleanup used by the `delete-account` Edge Function.
- Migration 025 adds a partial unique index preventing duplicate simultaneous active invitations for the same list/email pair.
- Existing schema includes profiles/users, lists, list_members, list_invitations, items, tombstones, list metadata timestamps, discovery and private realtime authorisation.
- `list_invitations` stores invited email, inviter, role/status fields and is protected by RLS. Invitees may read only their own active invitations; owners may read/manage invitations for their lists.
- `accept_list_invitation(target_invitation_id uuid)` validates the authenticated email against the invitation before creating/reactivating membership and marking the invitation accepted.
- Hosted Edge Function `invite-list` is deployed. It authenticates the owner, verifies ownership, creates/reuses the pending invitation row and triggers the normal Supabase passwordless `signInWithOtp()` flow for the invited email.
- Direct Resend invitation sending was intentionally removed from `invite-list`; authentication email generation/delivery is owned by Supabase Auth through configured Resend SMTP.
- `InvitationRepository.kt` now contains owner send plus recipient pending-invitation discovery/acceptance methods.
- Local Room UUIDs are reused as remote UUIDs for online-backed lists/items.
- `list_members.position` stores per-user list order; `items.position` stores shared item order.
- Normal reads hide deleted rows; controlled snapshots expose active/deleted/removed/not_found state.
- Sensitive membership/list-creation/deletion paths use controlled security-definer RPCs rather than unrestricted direct writes.
- Supabase URL/publishable key are supplied through BuildConfig/local.properties; publishable key may ship in APK. Never ship secret/service-role or Resend keys.

Account deletion backend added/tested 30 Aug 2026:
- `migrations/024_delete_online_account_data.sql` defines authenticated security-definer `jsimplelist.delete_online_account_data()`.
- Cleanup hard-deletes lists owned by the current account and their items/invitations/memberships; removes the current account from other owners' lists without deleting those lists/items; removes invitations created by the account and outstanding invitations addressed to the account email; removes the profile.
- Hosted Supabase Edge Function `delete-account` is deployed ACTIVE. It authenticates the caller using the forwarded Authorization header, runs the cleanup RPC as the caller, then uses server-side Supabase secret credentials to admin-delete exactly that Auth user.
- Supabase CLI 2.116.0 is installed locally as a root npm devDependency; project ref is `xislrkkifqayznsgwzno`; `supabase/.temp/` and root `node_modules/` are ignored. Hosted deploy works with `npx supabase functions deploy <function> --project-ref xislrkkifqayznsgwzno --use-api`; local `supabase status` needs Docker/Podman and is not required for hosted deploy.
- Android installs `supabase-functions`; `AuthRepository.deleteOnlineAccount()` invokes `delete-account`.

Invitation email/secrets state:
- Supabase Auth uses Resend SMTP with sender `jsimplelist@jobsheet.com.au`.
- A replacement Resend key was created on 30 Aug 2026, recorded locally in ignored `invite-secrets.env`, and configured in Supabase Auth SMTP.
- The same key was also stored as hosted Edge Function secret `RESEND_API_KEY` during development; current `invite-list` no longer consumes that secret, so later cleanup may remove it deliberately.
- `invite-secrets.env` is explicitly ignored and was verified with `git check-ignore -v invite-secrets.env`. Never commit it.

Important RPC/state contract:
- `create_online_list(...)` creates the list and owner membership while preserving the client UUID.
- `get_list_sync_snapshot(target_list_id uuid)` returns authoritative active/deleted/removed/not_found JSON including current membership/items.
- `get_my_online_lists()` returns active online memberships with list metadata, role and per-user position.
- `set_my_list_position()` changes current user's list order without changing another member's order.
- `delete_online_item(...)`, `rename_online_list(...)`, `delete_online_list(...)` and `leave_online_list(...)` remain the controlled write/delete paths.

Realtime contract:
- Per-list private topic: `jsimplelist:list:<list UUID>`.
- Authorised recipients are owner or active member.
- Item/list changes emit lightweight `list_changed` Broadcast notifications; clients load authoritative state rather than trusting payload rows.
- Each Android installation has persistent random client UUID; originating writes include it for self-suppression.
- Per-list realtime cannot discover an unknown list UUID; account/list-set realtime is still needed for instant create/share/remove discovery. Startup/sign-in/pull discovery remains fallback.

LIST SYNC FOUNDATION
--------------------
The Android online-list contract supports local-first writes, safe item deletion, list discovery, list metadata reconciliation, owner deletion/member leave, pull-to-refresh, per-list realtime signalling and account-scoped cached-list visibility.

Current behaviour:
- Local lists remain local and are never uploaded merely because the user signs in.
- `list_accounts` is now the account-specific authority for whether a cached online list belongs to the currently authenticated user and whether that user is owner/member.
- `discoverOnlineLists()` reads the current Supabase user ID, calls `get_my_online_lists()`, then after successful discovery clears that account's old `list_accounts` relationships and rebuilds them exactly from authoritative results.
- Relationship rebuilding occurs before list snapshot refreshes so a refresh failure does not leave only a partial set of account/list relationships restored.
- `loadVisibleLists(accountId)` returns device-local lists plus only that account's online lists, projecting the account-specific online role into returned `ListEntity.onlineState`.
- Signed-out visibility uses `loadVisibleLists(null)` and therefore shows only device-local lists.
- A local list made online records an `ONLINE_OWNER` `list_accounts` row for the current authenticated account.
- Remote owner/member lists absent from Room import into cache; existing cached list metadata reconciles by `updatedAt`; each discovered list then refreshes authoritative snapshot/items.
- Pull-to-refresh runs discovery/reconciliation; per-list realtime continues to trigger authoritative refresh.
- Add/edit/tick/untick write Room first then push when current rendered list is online; online item deletion uses local/server tombstones.
- Owner Delete list remains server-first; member Leave list remains server-first; local Delete remains Room-only.
- Full Room cache remains available offline, but account visibility rules prevent one account from seeing another account's shared-list cache.
- Last active list ID is persisted/restored safely when still visible.

Important remaining work:
- Complete the first-class invitation/onboarding handoff described in `docs/chatgpt/JSimpleList_INVITATION_FLOW.txt`.
- Recipient pending-invitation discovery/acceptance foundation builds, but the preferred one-click email -> web -> app authentication experience is not yet implemented.
- Owner remove-member controls are not yet implemented.
- Failed online writes retain Room change; durable retry/reconciliation is not yet implemented.
- Account-level realtime for list-set changes is not yet implemented.
- Android online-owner Rename is still local-only and must be wired to server rename RPC.
- Manual list ordering is not yet synced.
- Realtime bursts are not debounced/coalesced.

SHARING / INVITATION DIRECTION
------------------------------
- Owner-side Share list UX is implemented: same-account `Make available online` plus `Invite someone` by email.
- Invitation access is bound to the invited authenticated email/account; possession of a link or invitation UUID alone must never grant list access.
- Normal Supabase passwordless Auth is the single authentication path for invitation recipients; do not use Supabase Auth's separate Invite-user state as JSimpleList's product invitation authority.
- The preferred product workflow is now a first-class one-click handoff: invitation email -> branded HTTPS handoff on `jslist.jobsheet.com.au` -> verified Android App Link when installed -> Supabase authentication completion -> pending invitation -> Accept -> existing `discoverOnlineLists()` import as `ONLINE_MEMBER`.
- Manual six-digit OTP remains the fallback path.
- The website handoff must also handle app-not-installed and desktop/browser cases without presenting the unfinished browser client as complete.
- Detailed authority, URL, failure-state and implementation-boundary design is authoritative in `docs/chatgpt/JSimpleList_INVITATION_FLOW.txt`; do not duplicate it here.
- Browser same-account use requires no invitation: sign into `jslist.jobsheet.com.au` with the same account and online lists appear.


NEXT ANDROID / SHARING WORK
---------------------------

Current invitation/onboarding state:
- First-class invitation onboarding is now working end to end in real testing.
- A brand-new local list can be shared directly: Send invitation first makes the list online and only then sends the invitation.
- Invitation email uses the scanner-safe `https://jslist.jobsheet.com.au/invite` handoff.
- Supabase Auth redirects through `https://jslist.jobsheet.com.au/auth/invite`.
- Android verified App Links are working for the debug build; `/auth/invite` opens JSimpleList directly.
- `assetlinks.json` contains separate release and debug package statements.
- Before public 2.0 release, add the actual Google Play App Signing SHA-256 certificate fingerprint to the release Digital Asset Links statement.
- Real recipient testing with Kate successfully reached accepted shared-list membership.
- Migration 026 fixed accepted-member position assignment.
- Migration 027 adds item creator/editor attribution fields and is applied to hosted Supabase.
- Room schema v6 adds nullable item creator/editor attribution fields and has been built, installed and migration-tested on the development phone.

Immediate sharing/UI work:
1. Close the Share list dialog after a successful invitation.
   - Clear the invitation email.
   - Close only on success.
   - Leave the dialog open on failure so the user can retry.

2. Shared-list member viewer from the list title.
   - Tapping an online/shared list title should show:
     - owner
     - active members
     - pending invitations
   - This dialog should become the natural shared-list management surface.

3. Owner/member management.
   - Owner may cancel a pending invitation.
   - Owner may remove an active member.
   - Owner may unshare the list.
   - Member may leave the shared list.
   - Owner unshare should revoke active memberships/invitations and return the owner's copy to local-only.
   - Preserve sufficient removed/deleted/tombstone state so offline clients can learn that access ended.
   - Decide final server-copy cleanup timing separately; do not silently destroy offline state.

4. Persistent sharing notifications.
   - First event type: invitation accepted / member joined.
   - Notify the inviter/owner, e.g. `Kate joined Shopping list`.
   - Persist notifications so they are not lost while the app is closed.
   - Realtime may supplement this while the app is open but must not be the only delivery mechanism.
   - Future event types may include member left, member removed, invitation cancelled and list unshared.

5. Item creator/editor attribution.
   - Supabase items have nullable `created_by_user_id` and `updated_by_user_id`.
   - `created_by_user_id` is immutable once set.
   - `updated_by_user_id` changes whenever the item is edited, ticked/unticked, moved or tombstoned.
   - PostgreSQL sets/enforces attribution from `auth.uid()`; clients must not be authoritative for attribution.
   - Existing historic server items remain unattributed rather than guessed.
   - Room v6 adds nullable `createdByUserId` and `updatedByUserId`.
   - New online items should also record the current user ID locally immediately so the creator label does not need to wait for a remote refresh.
   - Item timestamps already exist as createdAt/updatedAt/deletedAt locally and created_at/updated_at/deleted_at remotely.

6. Item creator label.
   - Show on online/shared lists only.
   - Small, light-grey, low-emphasis text.
   - Right aligned in the item row.
   - Prefer the creator's chosen display name.
   - If no alias/display name was chosen, use the generated fallback such as `harrisk612@`.
   - Do not expose full email addresses merely to provide item attribution.
   - `updated_by_user_id` is stored for future use but does not need to be displayed initially.

7. Automatic fallback display name.
   - Display-name/alias entry becomes optional.
   - On first authenticated use, if no profile exists, create one automatically from the email local-part plus `@`.
   - Example: `harrisk612@example.com` -> `harrisk612@`.
   - The user may later replace the generated name with an alias such as `Kate` or `Josh`.
   - Duplicate generated names are acceptable initially; users can resolve rare collisions by choosing aliases.

Later sync/sharing work:
- Account/list-set realtime so new or removed memberships appear without manual discovery.
- Durable retry/reconciliation for failed online writes.
- Continue synced list ordering/manual ordering work.
- Refine browser membership/list-set refresh behaviour.
- Complete owner member controls through the shared-list information dialog.

Before public 2.0 release:
- Update About/version/build information that still reflects released 1.1.
- Add the Google Play App Signing SHA-256 fingerprint to `assetlinks.json`.
- Perform a real Google Play-signed 1.1 -> 2.0 upgrade test and verify local data preservation.
- Complete the external account-deletion resource/page required by Google Play.
- Update Google Play Data Safety for account/email/profile/shared-list processing.
- Reconcile README, store listing, privacy and product wording with network-enabled 2.0 behaviour.

BROWSER CLIENT
--------------
- Browser access uses the same Supabase account/list/membership/sync service as Android at `https://jslist.jobsheet.com.au`.
- Browser is another client, not a phone bridge and not a separate backend. It exposes online-backed lists only; local-only lists never leave the phone and therefore never appear in browser.
- A React + TypeScript + Vite client lives under `web/`.
- Browser Supabase configuration uses `VITE_SUPABASE_URL` and `VITE_SUPABASE_PUBLISHABLE_KEY`; real values live only in ignored local/deployment environment configuration.
- Email OTP sign-in works against the existing Supabase Auth account, and the browser session persists normally.
- The signed-in browser currently discovers active online lists through `list_members` plus `lists`, preserving per-user membership order.
- Opening a list calls `get_list_sync_snapshot(target_list_id)` and renders authoritative active items, Shopping quantity, completion state and the same incomplete-before-completed presentation.
- Browser item creation uses true INSERT, not UPSERT, because live testing proved INSERT follows the intended Android realtime trigger path.
- Browser item edit/tick/untick writes carry fresh `updated_at` and `origin_client_id`.
- Browser safe delete uses `delete_online_item`; Delete all uses the controlled bulk tombstone path.
- Browser bulk Uncheck all controls are implemented.
- Browser inline list rename is implemented: click the list name to edit; Enter/blur saves, Escape cancels, blank trimmed input does not save.
- Browser list rename uses the controlled `rename_online_list` RPC and list UPDATE realtime.
- Browser list headings use a small muted chain/link SVG beside the stored list name to indicate online status. Android now uses the same path geometry for visual consistency.
- Browser list-selector UI distinguishes list type and Owner/Shared role.
- Browser list names are not modified to include status glyphs; status remains a separate visual element.
- Each browser installation has a persistent random UUID stored in localStorage. Browser writes use it as `origin_client_id`.
- The browser subscribes to the same private per-list `list_changed` Realtime contract as Android and ignores matching own-origin broadcasts.
- Same-account browser <-> Android item changes have been proven for tick/untick, add, edit and safe delete.
- Public hosting is DigitalOcean App Platform Static Site in SYD1, app `jsimplelist`, component `jsimplelist-web`, source `web/`, branch `main`, autodeploy enabled, build command `npm install && npm run build`, output directory `dist`.
- Production configuration exposes only the public Supabase publishable key, never a secret/service-role key.

Current browser limitations / next work:
- Browser cannot yet create lists. Browser-created lists should be online/shared-to-self from birth; browser does not need a local/unshare concept.
- A remotely deleted selected list can remain visibly stale because `openOnlineList` currently sets `List is no longer available` for an inactive snapshot but does not clear `selectedSnapshot` or reload `onlineLists`. It eventually disappears when list discovery reruns (for example after session refresh), but immediate eviction/reload is required.
- Browser list discovery is not account-level realtime. New/removed memberships or entirely new list UUIDs require discovery to rerun.
- Browser does not remember the last-opened list by design.
- Browser Undo for bulk Uncheck all remains a possible follow-up; Android already has conservative Undo.

SETTINGS / PREFERENCES
----------------------
Do not add Settings merely to expose options that are not genuinely useful.

Current decisions:
- Keep pinch-to-resize as-is; no text-size lock/reset setting required now.
- Remember the last active list and restore that list when the app starts. Persist the list ID locally; if the saved list no longer exists, fall back safely to an available list. This is normal app behaviour, not a user-selectable default-list preference.
- Completed items below incomplete items is normal list behaviour, not a preference.
- Manual item ordering is list functionality, not a setting.
- Manual list ordering is planned functionality, not a setting.
- Possible 2.1 preference: allow unused list types to be hidden from the New list chooser. Existing lists of a disabled type remain untouched.
- A possible future ordering preference is new items top/bottom; current intended default is top.
- A possible future ordering preference is checked items move to bottom/remain in place; current intended default is bottom.
- Add Settings only when there are enough genuinely useful preferences/features to justify it.

PUBLIC WEBSITE / LEGAL / PLAY STORE
-----------------------------------
- Public JSimpleList product page: `https://jobsheet.com.au/jslist/`.
- Legacy landing path `https://jobsheet.com.au/jsimplelist/` is retained as a redirect to `/jslist/`.
- Browser app: `https://jslist.jobsheet.com.au`.
- Privacy Policy URL intentionally remains `https://jobsheet.com.au/jsimplelist-privacy.html` because it is already registered with Google Play.
- Terms of Use URL intentionally remains `https://jobsheet.com.au/jsimplelist-terms.html`.
- JSimpleList Privacy and Terms pages use JSimpleList branding/navigation rather than the JobSheet sidebar. JobSheet / 21 TWELVE CONSULTING PTY LTD remains identified in the legal text as the producer/legal entity.
- Current legal documents cover local-only lists, optional online accounts, private sharing/synchronisation, service communications, future service/pricing changes, account/data handling and South Australian governing law.
- Current product-page positioning: `Simple list keeping`, local or privately shared lists, browser access, `No ads. No fees.` as a present-state statement rather than a permanent promise.
- Google Play Website field is set to `https://jobsheet.com.au/jslist/`.
- Google Play Privacy Policy field is confirmed as `https://jobsheet.com.au/jsimplelist-privacy.html`.
- Before submitting 2.0, review and update Google Play Data safety because 2.0 introduces optional account/email/display-name data, Supabase-backed online/shared list data, invitations/memberships and network processing that did not exist in released 1.1.
- Before public 2.0 release, also reconcile README/store-listing wording so released metadata no longer describes JSimpleList as strictly local-only.

FUNDING / COST FUTURE-PROOFING
------------------------------
- Local-only use should remain free.
- Online sync/sharing/browser access may initially be free while costs are negligible.
- If recurring backend cost becomes meaningful, preferred model is a small annual fee, notionally around US$5/year, rather than a monthly subscription.
- Voluntary donations/support may exist separately.
- Do not hard-wire billing into list operations. Keep an entitlement boundary for online service.
- Leave room for future states such as free, supporter, paid-online, promotional or admin-granted.
- Leave room for operational safeguards: account/list/member/item limits, disable new online registrations, disable new online-list creation, suspend sync/realtime while preserving local copies.
- Popularity/back-end failure must degrade safely: local lists remain usable and no local data is lost.

PROJECT WORKING PROTOCOL / DOCUMENTS
------------------------------------
- `docs/chatgpt/JSimpleList_RULES.txt` is the authoritative durable working, source-editing, validation, secrets, Git and session-closeout protocol.
- `docs/chatgpt/JSimpleList_CONTEXT.txt` is the concise current architecture/state/continuation reference.
- `docs/chatgpt/JSimpleList_INVITATION_FLOW.txt` is the authoritative subsystem design for first-class invitation/onboarding.
- ChatGPT should handle complete project-document replacements when the current documents are available rather than handing piecemeal maintenance back to the user.
- Remove/rewrite stale current-state and NEXT TASK material instead of accumulating contradictory append-only history.
- Before broad rediscovery, first check whether one of the authoritative JSimpleList documents already owns the needed information.
- Session closeout follows `JSimpleList_RULES.txt`: validate, diff-check, inspect status/diff, update authoritative documents, explicitly stage, commit/push a cohesive checkpoint, and record the exact next task.

CMD / TOOLING
-------------
Assume Windows CMD, normally from:
    D:\SimpleList>

Use CMD-compatible one-line commands and `&&` when dependent commands should run sequentially.
Prefer `git --no-pager ...` where supported.

Normal debug workflow:
    build_debug && install_debug

Tracked helper scripts in the repository root:
- `build_debug.bat` runs `gradlew.bat assembleDebug` and then `git diff --check`.
- `install_debug.bat` installs `app\build\outputs\apk\debug\app-debug.apk` using the Android SDK ADB path.
- The helpers exist specifically so normal development does not require remembering long SDK/APK command strings.

Underlying build command if needed:
    gradlew.bat assembleDebug

Build signed release:
    gradlew.bat assembleRelease

Install signed release:
    "%LOCALAPPDATA%\Android\Sdk\platform-tools\adb.exe" install -r app\build\outputs\apk\release\app-release.apk

Check status/history:
    git status --short
    git --no-pager log --oneline -5

Important:
- Build before committing.
- Test important UI behaviour on the real Android phone.
- Run `git diff --check` before commits.
- Do not assume an old APK at the output path is a successful new build after a failed Gradle run.
- Real phone remains preferred over emulator.
- Keep unrelated Android Studio/.idea noise out of functional commits.
- `local.properties` is local-only and must remain untracked.
- `audit.cmd` is local-only.
- Avoid broad `git add` commands; explicitly stage intended files.

SIGNING / FILE SAFETY
---------------------
Permanent local signing key:
    D:\simplelist\keys\jsimplelist-release.jks
Alias:
    jsimplelist

Release certificate SHA-256:
    fc11217128f365366d4c3f988a2536d40251f6e71b5275b611896c749206af59

Never commit:
- .jks files
- signing.properties
- keystore passwords
- key passwords
- Supabase secret/service-role keys
- Resend API keys
- generated APK/release output

Local-only/untracked areas include:
- signing.properties
- keys/
- app/release/
- .idea/inspectionProfiles/
- audit.cmd
- reset-adb.bat

PUBLIC REPOSITORY / LICENCE
---------------------------
Repository:
    https://github.com/jobsheet-aus/JSimpleList
Branch:
    main

Repository is public.
Current licence: MIT.
Copyright owner:
    21 TWELVE CONSULTING PTY LTD

Existing MIT releases retain their granted MIT rights if future versions use different licensing terms.
CONTRIBUTING.md preserves practical future relicensing flexibility for substantial contributions; seek proper legal advice/formal CLA if third-party contributions become commercially important.

README currently describes the released local-only app. Update README/privacy/listing wording before a public network-enabled 2.0 release.

CURRENT GIT CHECKPOINT - 30 AUG 2026
------------------------------------
Functional/account/sharing/invitation checkpoint committed locally:
    f7b5546 (HEAD -> main) Add account-scoped sharing and invitation foundation

Previous pushed checkpoint:
    432361b (origin/main, origin/HEAD) Update JSimpleList 29 Aug session context
    b5ab29c Refine Android list navigation and sharing UI

Commit `f7b5546` contains the 30 Aug Android/backend implementation, including:
- Room v5 account-scoped `list_accounts`
- persisted-session restoration fix and sign-out fallback
- account deletion client/backend path
- Share list / New list UX changes
- owner-side invitation send path
- recipient pending-invitation foundation
- migrations 024 and 025
- Supabase Edge Functions `delete-account` and `invite-list`
- local Supabase CLI npm dependency
- associated build/schema/configuration changes

Remaining uncommitted closeout files at this documentation checkpoint:
- `.gitignore` with `/reset-adb.bat` added beside `/audit.cmd`
- `docs/chatgpt/JSimpleList_RULES.txt`
- `docs/chatgpt/JSimpleList_INVITATION_FLOW.txt`
- this `docs/chatgpt/JSimpleList_CONTEXT.txt`

`reset-adb.bat` is intentionally local-only and ignored. `git check-ignore -v reset-adb.bat` confirmed the repository rule.

Ignored/local-only items include `reset-adb.bat`, `invite-secrets.env`, `node_modules/`, `supabase/.temp/`, signing material and `local.properties`. Explicit staging only; do not broad-add.

The final closeout commit should contain only the intended `.gitignore` rule and the three maintained project documents, then be pushed together with `f7b5546`.

SESSION CLOSEOUT - 30 AUG 2026
------------------------------
Major work/decisions represented by this checkpoint:
- Completed the account-scoped Room v5 model using `list_accounts(listId, accountId, onlineState)` so online cache visibility follows the authenticated account while local lists remain device-local.
- Fixed persisted Supabase session restoration by awaiting Auth session initialisation before reading the current session.
- Kept the supabase-kt 3.2.1 sign-out fallback to `clearSession()` for remote sign-out failures.
- Corrected Android online-account deletion cleanup for the account-scoped model and successfully tested disposable-account deletion end to end against the deployed migration 024 / `delete-account` backend.
- Tidied Sharing/profile UI: removed obsolete `Disable sharing`, kept display-name editing, and exposed separate Sign out and destructive Delete account actions.
- Moved `New list` into the hamburger and promoted current-list `Share list`.
- Implemented same-account `Make available online` and removed the duplicate Manage-lists Share path.
- Added migration 025 to prevent duplicate active invitations for the same list/email and applied it to hosted Supabase.
- Added/deployed `invite-list` Edge Function. Initial direct Resend delivery was replaced with normal Supabase `signInWithOtp()` so invitation recipients use the same passwordless authentication path as ordinary sign-in.
- Rotated/recorded the JSimpleList Resend key, updated Supabase Auth SMTP, verified the local secret file is ignored, and confirmed the new Supabase OTP invitation email arrives.
- Added owner-side invitation email UI and modular `InvitationRepository`.
- Added recipient pending-invitation discovery/acceptance foundation. `accept_list_invitation()` remains the membership authority; accepted lists reuse `discoverOnlineLists()` and map member role to `ONLINE_MEMBER`.
- Android debug build succeeds after these changes and the debug APK has been installed successfully.
- Real testing exposed the remaining product-level gap: a fresh recipient should not have to reason out email + OTP manually. The accepted product direction is now a first-class one-click invitation onboarding workflow using Supabase authentication, the controlled JSimpleList website and verified Android App Links, with OTP retained as fallback.
- Created `JSimpleList_RULES.txt` to make JSimpleList's working/session protocol authoritative in-repo.
- Created `JSimpleList_INVITATION_FLOW.txt` as the authoritative invitation/onboarding subsystem design.
- Committed the complete functional/account/sharing/invitation implementation checkpoint as `f7b5546 Add account-scoped sharing and invitation foundation`.
- Kept `reset-adb.bat` as a useful local helper but deliberately ignored it alongside `audit.cmd` rather than adding it to project history.
- Browser `jslist.jobsheet.com.au` is publicly reachable but still cannot create lists. An unsolicited external signup demonstrated that the unfinished browser experience can be encountered; do not present the browser client as complete until its basic onboarding/list-creation path is ready.

NEXT TASK
---------
Begin a fresh session titled/treated as:
    JSimpleList first-class invitation onboarding architecture

Read first:
- `docs/chatgpt/JSimpleList_RULES.txt`
- `docs/chatgpt/JSimpleList_CONTEXT.txt`
- `docs/chatgpt/JSimpleList_INVITATION_FLOW.txt`

Do not begin implementation immediately. The architecture phase must first determine:
1. exact public invitation/handoff URL(s) on `jslist.jobsheet.com.au`
2. exact Supabase token/auth-state lifecycle and where one-time state is consumed
3. email-scanner-safe website handoff behaviour
4. installed-app, not-installed and desktop/browser outcomes
5. Android verified App Link / `assetlinks.json` / signing-certificate contract
6. Android auth-completion and wrong-account handling
7. fallback OTP behaviour and recovery states
8. security contract proving that URL/token possession alone never grants list membership

Only after that end-to-end contract is settled should implementation proceed through the boundaries in `JSimpleList_INVITATION_FLOW.txt`.

SOURCE-EDITING REMINDER FOR NEXT SESSION
----------------------------------------
Read and follow `docs/chatgpt/JSimpleList_RULES.txt`; it is authoritative.
